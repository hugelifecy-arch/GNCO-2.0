# Changelog

## 2026-02-18
- Added `/signin` page so navigation no longer leads to a 404 and explicitly labels authentication as coming soon.
- Added legal pages and stable routes for `/privacy-policy`, `/terms-of-service`, and `/data-architecture`; updated footer links accordingly.
- Added demo dataset disclosure across app routes and replaced personal-looking placeholder identity text with `Demo User`.
- Added jurisdiction detail routes at `/coverage/[jurisdictionId]` with per-jurisdiction `Last updated` and `Sources` references.
- Added coverage page trust metadata note and direct links to jurisdiction details.
- Upgraded shared architect results page to a read-only MVP summary view with explicit export scaffolding marked as coming soon.
- Marked non-implemented export/upload actions in operator modules as disabled with `Coming soon` tooltips.
- Added data-source metadata files for coverage and jurisdictions.
